spring:
  datasource:
    url: jdbc:mysql://localhost:3306/database?useSSL=false&serverTimezone=Asia/Jakarta
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
